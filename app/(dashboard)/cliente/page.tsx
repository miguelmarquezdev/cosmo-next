import{createClient}from"@/lib/supabase/server";import{Card,CardContent,CardHeader,CardTitle}from"@/components/ui/card";import{Badge}from"@/components/ui/badge";import{Progress}from"@/components/ui/progress";export default async function Page(){const s=await createClient();const{data:{user}}=await s.auth.getUser();const{data:projects}=user?await s.from('projects').select('*').eq('client_id',user.id).limit(5):{data:[]};const list=projects?.length?projects:[{name:'Tu proyecto aparecerá aquí',status:'planning',progress:0}];return <><div><h1 className="text-3xl font-bold">Hola, {user?.user_metadata?.full_name||'bienvenido'}</h1><p className="mt-1 text-muted-foreground">Consulta el estado de tus servicios y próximos pasos.</p></div><div className="mt-8 grid gap-4 md:grid-cols-3">{[['Proyectos activos',String(projects?.length||0)],['Tareas pendientes','0'],['Archivos nuevos','0']].map(x=><Card key={x[0]}><CardHeader><p className="text-sm text-muted-foreground">{x[0]}</p><CardTitle className="text-3xl">{x[1]}</CardTitle></CardHeader></Card>)}</div><Card className="mt-6"><CardHeader><CardTitle>Servicios y proyectos</CardTitle></CardHeader><CardContent className="grid gap-4">{list.map((p:any)=><div key={p.name} className="rounded-xl border p-5"><div className="flex items-center justify-between"><div><p className="font-semibold">{p.name}</p><p className="text-sm text-muted-foreground">Estado actual</p></div><Badge>{p.status}</Badge></div><div className="mt-4"><Progress value={p.progress}/></div></div>)}</CardContent></Card></>}